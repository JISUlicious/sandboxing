#!/usr/bin/env bash
# Wrapper invoked by the control plane for SPEC-302 quota teardown.
# See deploy/sandbox-quota-helper.sh for the privileged implementation.

set -euo pipefail
exec sudo -n /usr/local/bin/sandbox-quota-helper teardown
