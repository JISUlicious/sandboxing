# /etc/sandbox/iptables.env — overrides for deploy/iptables-setup.sh.
# Copy to /etc/sandbox/iptables.env on the host (next to the existing
# `env` file). Uncomment to override defaults.
#
# Defaults:
#   SANDBOX_SUBNET=172.30.0.0/24
#   PROXY_IP=172.30.0.2
#   PROXY_PORT=3128

#SANDBOX_SUBNET=172.30.0.0/24
#PROXY_IP=172.30.0.2
#PROXY_PORT=3128
